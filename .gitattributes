# Normalize text; keep platform-correct line endings for scripts.
* text=auto

*.sh   text eol=lf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# Prebuilt avra binaries are committed on purpose.
*.exe            binary
macos/avra/avra  binary
