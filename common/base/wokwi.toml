[wokwi]
version = 1
# arduino-cli produces <sketch>.ino.hex/.elf; build.ps1/build.sh normalize those to
# these stable names so this file stays the same across every project.
firmware = 'build/firmware.hex'
elf = 'build/firmware.elf'
